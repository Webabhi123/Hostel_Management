@model IEnumerable<Hostel_Management.Models.Staff>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutSideNavbar.cshtml";
}
<p>
    @Html.ActionLink("Create", "Create", new { area = "Admin", controller = "Staff" }, new { @class = "btn btn-info" })
</p>
<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>PhoneNumber</th>
            <th>Email</th>
            <th>Address</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.PhoneNumber</td>
                <td>@item.Email</td>
                <td>@item.Address</td>
                <td>
                    @* <!-- Edit Button -->
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">Edit</a>

                <!-- Delete Button -->
                <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;">
                <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                </form> *@
                    @Html.ActionLink("Edit", "Edit", new { id = item.StaffId }, new { @class = "btn btn-warning" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.StaffId }, new { @class = "btn btn-danger delete-link", data_id = item.StaffId })

                </td>
            </tr>
        }
    </tbody>
</table>

<script>

    $(document).ready(function () {
        $(".delete-link").on("click", function (e) {
            e.preventDefault();
            var id = $(this).data("id");
            var confirmation = confirm("are you sure do you want to delete this?");

            if (confirmation) {
                window.location.href = "/Admin/Staff/Delete?id=" + id;
            }

        })
    })
</script>

